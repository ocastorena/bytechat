export const ROUTES = {
  HOME: "/home",
  EXPLORE: "/explore",
  LISTS: "/lists",
  PROFILE: "/profile",
  LOGIN: "/login",
  SIGNUP: "/signup",
} as const

export const PAGINATION = {
  PAGE_SIZE: 10,
  REFRESH_INTERVAL: 10000,
} as const
