[build]
buildCommand = "npm run build && npx tsx prisma/seed.ts"

[deploy]
startCommand = "npx prisma migrate deploy && npm start"
healthcheckPath = "/login"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
